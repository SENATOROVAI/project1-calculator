from divide_zero import divide
from operations import add

assert divide(0, 2) == 0, "0 / num2 should be zero !"
assert divide(0, 0) == None, "Devision by zero not allowed !"
assert add(2, 2) == 4, "Correct" 
